POSTGRES_USER=""
POSTGRES_PASSWORD=""
POSTGRES_DB=""

DATABASE_URL=""

# Host path for Postgres data, e.g. /opt/docker/coin-guard/postgres-data on the Homelab,
# or ./.postgres-data locally if that path isn't shared with Docker Desktop.
POSTGRES_DATA_PATH=""
